#include<stdio.h>
int main()
{
    int length,breadth;
    printf("Enter the length of rectangle in metre\n", length);
    scanf("%d",&length);
    printf("Enter the breadth of rectangle in metre\n", breadth);
    scanf("%d",&breadth);
    printf("the area of rectangle is %d metre", length*breadth);
    return 0;
}